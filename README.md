# DL_CA1_CNN
To design and implement a Convolutional Neural Network (CNN) model for binary image classification that can accurately classify input images as either a cat or a dog using a labeled dataset.
